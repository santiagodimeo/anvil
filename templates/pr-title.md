`type(scope): imperative description`

- Same `type` and `scope` as the primary commit (see references/commit-style.md)
- One line, no trailing period, no emoji
- Do not restate the title in the body
