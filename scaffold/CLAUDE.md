# {{PROJECT_NAME}}

{{ONE_LINER}}

Status: **{{STATUS}}** (grounded {{DATE}}). Next: `/blueprint`.
Plan: **`docs/product/roadmap.md`**.

## Stack

{{STACK}}

## Architecture

<!-- The one load-bearing idea: the seam, the contract, the boundary others must respect.
     Keep this to the invariant that must not be violated. Fill it in /blueprint. -->
_TODO — capture the core architectural invariant here (see `/blueprint`)._

## Commands

<!-- The handful of commands to run / build / test this project. -->
_TODO — list run / build / test commands (e.g. `{{VERIFY_CMD}}`)._

## Conventions

- Gitflow branches (`type/short-description`); conventional commits (`type(scope): subject`).
- Decisions, not options-dumps — name the alternative and the one-line reason it lost.
